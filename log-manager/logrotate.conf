/var/log/nginx/blocklist.log {
    daily
    rotate 30
    compress
    delaycompress
    missingok
    notifempty
    # copytruncate copies the log then truncates it in-place so nginx keeps
    # writing to the same open fd without needing a signal from this container.
    copytruncate
}
