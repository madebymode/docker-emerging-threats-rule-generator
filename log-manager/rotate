#!/bin/sh
# Executed daily by busybox crond (/etc/periodic/daily/).
# State file lives on the shared nginx-logs volume so it survives container
# restarts and prevents double-rotation within the same day.
set -e

logrotate -s /var/log/nginx/.logrotate.status /etc/logrotate.d/nginx-blocklist
